#include <iostream>
#include "network.h"

using namespace std;


int main()
{
	network network1;
	vector<neuron>::iterator it = network1.neurons.insert(network1.neurons.end(), true);
	it->
    cout << "Hello world!" << endl;
    return 0;
}
